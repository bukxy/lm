{% extends 'back/baseAdmin.html.twig' %}

{% block title %}
    {% if editMode %}
        Edition du boost
    {% else %}
        Ajout d'un boost
    {% endif %}
{% endblock %}

{% block category %}
    {% if editMode %}
        Edition du boost
    {% else %}
        Ajout d'un boost
    {% endif %}
{% endblock %}

{% block body %}
    {{ form_start(formImage) }}

        {{ form_row(formImage.name, {'label': 'Selectionner une image'}) }}
        {{ form_row(formImage.alt, {'label': 'Description'}) }}  
        {{ form_row(formImage.imageCat, {'label': 'Catégorie de l\'image'}) }}

        <button type="submit" class="btn btn-primary">
            {% if editMode %}
                Sauvagarder
            {% else %}
                Ajouter
            {% endif %}
        </button>

    {{ form_end(formImage) }}
{% endblock %}
